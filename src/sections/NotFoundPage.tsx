import { SectionShell } from "../components/SectionShell";

export default function NotFoundPage() {
  return (
    <SectionShell
      eyebrow="404"
      title="This page doesn't exist yet."
      tagline="Gunakan tanda di pojok layar untuk kembali ke beranda."
      background="cream"
      status="Halaman tidak ditemukan"
    />
  );
}
